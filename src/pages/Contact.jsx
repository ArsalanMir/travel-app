import React from 'react';
import BookingForm from '../components/BookingForm';

const Contact = () => {
  return (
    <section className="pt-24"> {/* Adds space to prevent overlap */}
      <BookingForm />
    </section>
  );
};

export default Contact;
